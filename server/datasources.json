{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb": {
    "host": "${MONGOHOST}",
    "port": 27017,
    "database": "demo_ibpms",
    "password": "",
    "name": "mongodb",
    "user": "",
    "connector": "mongodb"
  },
  "ibpms": {
    "host": "${DBHOST}",
    "port": 3306,
    "database": "${DATABASE}",
    "password": "${PASSWORD}",
    "name": "ibpms",
    "user": "${USER}",
    "connector": "mysql"
  }
}
